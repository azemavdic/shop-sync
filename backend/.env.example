# Copy from root .env or set these when running backend
# Backend loads from root .env when run from backend/ directory

DATABASE_URL="postgresql://shopsyncx:shopsyncx_secret@localhost:5432/shopsyncx?schema=public"
JWT_SECRET="change-this-to-a-random-secret-in-production"
PORT=3000
NODE_ENV=development
